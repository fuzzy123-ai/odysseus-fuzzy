# Frozen local Faster-Whisper runtime for CPython 3.11 Linux images.
# This is a separate lock, intentionally installed only through the container
# image INSTALL_STT switch. It has no provider/client package and no model payload.
faster-whisper==1.2.1
ctranslate2==4.8.1
av==18.0.0
huggingface-hub==0.36.2
tokenizers==0.23.1
onnxruntime==1.27.0
tqdm==4.69.0
filelock==3.19.1
fsspec==2025.9.0
hf-xet==1.1.10
httpx==0.28.1
httpcore==1.0.9
anyio==4.10.0
h11==0.16.0
sniffio==1.3.1
packaging==25.0
# Keep the STT lock compatible with Kubernetes, a transitive core dependency
# of ChromaDB.  Kubernetes now requires PyYAML >= 6.0.3.
PyYAML==6.0.3
requests==2.32.5
typing_extensions==4.15.0
certifi==2025.8.3
charset-normalizer==3.4.3
# httpx2, used by the test-client dependency in requirements.txt, requires
# idna >= 3.18.  Do not let this optional STT lock downgrade the core stack.
idna==3.18
urllib3==2.5.0
coloredlogs==15.0.1
humanfriendly==10.0
flatbuffers==25.2.10
numpy==2.3.2
# ChromaDB's transitive googleapis-common-protos dependency now requires
# protobuf >= 6.33.5.  Keep the optional STT lock from downgrading it.
protobuf==6.33.5
sympy==1.14.0
mpmath==1.3.0
setuptools==80.9.0
