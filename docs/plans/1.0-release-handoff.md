# 1.0 Release Handoff

Stand: 2026-06-19

Status: **accepted Go evidence; final external review remains separate from deploy/tag/distribution**

## Decision

Charlie records the `1.0.0` release handoff as accepted Go evidence.

This means:

- Odysseus is internally release-candidate-ready.
- The automated release gates are documented as green.
- Fresh install, upgrade path, and known limits are documented.
- External `1.0.0` evidence can proceed to final review.
- Provider Proof and Test-Vault Export/Import/Rebuild are closed by isolated redacted evidence from 2026-06-19.
- Deploy, tag, distribution, and unrelated live gates still require separate operator approval.

## Evidence Summary

Current documented evidence:

- Automated REL1 gates: `235 passed, 44 warnings`.
- ABC0-ABC3 release evidence gates are completed and committed.
- Fresh install evidence exists in `docs/plans/1.0-manual-release-evidence-log.md`.
- Upgrade path evidence exists in `docs/plans/1.0-manual-release-evidence-log.md`.
- Known limits are documented in `docs/plans/1.0-evidence-release-checklist.md`.
- Release decision rules are documented in `docs/plans/1.0-release-decision-bundle.md`.
- Current read-only handoff renderer reports external release go as `true`.

Open blockers for external `1.0.0` evidence:

- none in the manual evidence model.
- `REL-final-external-review`: final review of evidence wording before any public release communication.
- Deploy, tag, and distribution remain separate actions, not implied by this handoff.

## Worktree And Remote State

Observed before this handoff:

```text
Branch: dev
Local HEAD before handoff record: 71b5b84d Add homeserver ops follow-up tooling
Worktree: only docs/plans/1.1-private-source-ops-roadmap.md was untracked
origin: https://github.com/pewdiepie-archdaemon/odysseus.git
fork: fuzzy -> https://github.com/fuzzy123-ai/odysseus-fuzzy.git
```

Remote note:

- The correct upstream/original repository is `origin`.
- The correct fork remote is `fuzzy`.
- The current local GitHub credentials still returned `403` for `origin`.
- The latest clean local commits were pushed to `fuzzy/dev`.

## Approved Next Scope

The accepted Go evidence handoff opens the final review path:

- `REL-final-external-review`

Allowed scope:

- final evidence wording review
- docs-only policy work
- no deploy, tag, distribution, live Nextcloud, Telegram, host mutation, backup, restore, or unrelated network action without a separate Go
- no secrets, tokens, chat IDs, passwords, raw provider output, or private source content in docs/tests/logs/handoffs

Blocked until separate explicit operator Go and evidence:

- public `1.0.0` release announcement after final review
- additional live Provider Proof rerun
- additional live Test-Vault Export/Import/Rebuild rerun
- live Nextcloud writes
- live Telegram send/download/STT
- host backup/restore/deploy/update action

## Go Partial No-Go

`Go`:

- Granted for external `1.0.0` evidence review.

`Partial`:

- Future state only if required evidence regresses or is superseded.

`No-Go`:

- Applies if Provider Proof, Test-Vault evidence, or secret-safety boundaries regress.

`Deferred`:

- Live Nextcloud integration, live Telegram Voice, server-side Nextcloud app, Postgres live migration, accelerator/research tracks, and any host mutation.

## Charlie Handoff Card

Status: accepted Go evidence

Changed files expected in this handoff:

- `docs/plans/1.0-release-handoff.md`
- `docs/plans/1.0-manual-release-evidence-log.md`
- `docs/plans/1.0-evidence-release-checklist.md`
- `docs/plans/1.1-private-source-ops-roadmap.md`

Tests:

- Release handoff/readiness tests should remain green.
- Docs-only changes do not perform live checks.

Next action:

- Commit this handoff record.
- Push to `fuzzy/dev` if `origin` remains blocked by local credentials.
- Run `REL-final-external-review` after the commit is clean.
